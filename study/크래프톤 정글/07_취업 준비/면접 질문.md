 StoLink (정글 최종 프로젝트)

  OAuth2 / JWT (아까 다뤘으니 간략하게)
  1. Refresh Token Rotation에서 Grace Period 10초의 근거는? 너무 길거나 짧으면?
  2. Cross-Domain 쿠키 공유 문제를 어떻게 해결했는지 구체적으로

  결제 (토스페이먼츠)
  3. 비관적 락을 선택한 이유는? 낙관적 락과 비교해서 왜?
  4. 외부 PG 호출을 트랜잭션 밖으로 분리했다는데, 그러면 PG는 성공했는데 DB
  반영이 실패하면? (보상 트랜잭션)
  5. 멱등키는 어디에 저장하고, 만료는 어떻게 처리했나?

  N+1
  6. DTO Projection과 Fetch Join을 언제 각각 쓰는지 기준은?
  7. default_batch_fetch_size=100에서 100인 이유? 너무 크면 문제는?

  Neo4j
  8. 왜 관계형 DB가 아닌 그래프 DB를 선택했나? 관계형으로도 가능하지 않았나?

  프론트엔드
  9. SVG → Canvas 전환 시 잃는 것은? (접근성, 이벤트 핸들링 등)
  10. 코드 스플리팅에서 hover 시 preload하는 방식의 단점은?

  ---
  AiDiary (캡스톤)

  RabbitMQ (포트폴리오에서 가장 상세하게 적힌 부분 — 깊게 물어볼 확률 높음)
  11. @Async로도 스레드 고갈은 해결됐는데 왜 RabbitMQ까지 도입했나? (Scale-out
  문제)
  12. DLQ에 들어간 메시지는 어떻게 처리하나? 자동 재처리? 수동?
  13. at-least-once vs exactly-once 차이, 왜 at-least-once를 선택했나?
  14. 멱등성 3중 가드에서, 하나만 있으면 안 되나? 왜 3개씩이나?
  15. Worker가 죽었는지 어떻게 감지하나? 헬스체크는?

  Redis 캐싱
  16. 캐시 키를 날짜 기반으로 했는데, 서버 시간대가 다르면?
  17. Cache Stampede (동시에 캐시 미스) 문제는 고려했나?

  순환 참조
  18. @Data를 왜 처음에 썼고, 제거 후 뭘로 대체했나? Lombok 사용 기준은?

  ---
  Knowledge Garden (포트폴리오 사이트)

  19. MongoDB를 선택한 이유? PostgreSQL 대비 장점이 이 프로젝트에서 뭐였나?
  20. LLM 가드레일 분류기는 어떤 기준으로 필터링하나? 오탐률은?

  ---
  공통 / 인성

  21. 크래프톤 정글에서 Pintos 개발했다는데, OS 커널 레벨에서 배운 게 실제
  개발에 어떻게 도움?
  22. Claude Code / Antigravity 같은 AI 도구를 쓴다고 했는데, AI 도구를 쓸 때의
  한계나 주의점은?
  23. 5인 팀에서 기획 + 백엔드 + 프론트를 다 했는데, 역할 분담은 어떻게 했나?