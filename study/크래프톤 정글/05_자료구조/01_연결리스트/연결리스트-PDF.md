---
date: 2025-10-02
tags: [CS]
---


# 📘 Section A – Linked Lists 문제 정리

---

## 1. insertSortedLL

사용자로부터 정수를 입력받아 **오름차순**으로 연결 리스트에 삽입하는 함수.  
이미 존재하는 값은 삽입할 수 없다.

- **삽입 성공** → 삽입된 인덱스 반환
    
- **삽입 실패** → `-1` 반환
    
- 연결 리스트는 정렬된 상태이거나 비어 있다고 가정한다.
    

```c
int insertSortedLL(LinkedList *ll, int item);
```

✅ 예시

- 리스트: `2, 3, 5, 7, 9`  
    → `insertSortedLL(ll, 8)` → `2, 3, 5, 7, 8, 9`  
    → 반환: `4`
    
- 리스트: `5, 7, 9, 11, 15`  
    → `insertSortedLL(ll, 7)` → `5, 7, 9, 11, 15` (삽입 안됨)  
    → 반환: `-1`
    

---

## 2. alternateMergeLL

두 번째 리스트의 노드를 첫 번째 리스트의 **교대 위치(alternate positions)**에 삽입한다.  
두 번째 리스트의 노드는 첫 번째 리스트에 교대 위치가 있을 때만 삽입한다.

```c
void alternateMergeLL(LinkedList *ll1, LinkedList *ll2);
```

✅ 예시

- L1: `1, 2, 3`
    
- L2: `4, 5, 6, 7`  
    → 결과:
    
    - L1: `1, 4, 2, 5, 3, 6`
        
    - L2: `7`
        

---

## 3. moveOddItemsToBackLL

리스트 안의 **홀수 노드**들을 모두 리스트의 뒤쪽으로 이동한다.

```c
void moveOddItemsToBackLL(LinkedList *ll);
```

✅ 예시

- 입력: `2, 3, 4, 7, 15, 18` → 출력: `2, 4, 18, 3, 7, 15`
    
- 입력: `2, 7, 18, 3, 4, 15` → 출력: `2, 18, 4, 7, 3, 15`
    
- 입력: `1, 3, 5` → `1, 3, 5` (변화 없음)
    
- 입력: `2, 4, 6` → `2, 4, 6` (변화 없음)
    

---

## 4. moveEvenItemsToBackLL

리스트 안의 **짝수 노드**들을 모두 리스트의 뒤쪽으로 이동한다.

```c
void moveEvenItemsToBackLL(LinkedList *ll);
```

✅ 예시

- 입력: `2, 3, 4, 7, 15, 18` → 출력: `3, 7, 15, 2, 4, 18`
    
- 입력: `2, 7, 18, 3, 4, 15` → 출력: `7, 3, 15, 2, 18, 4`
    
- 입력: `1, 3, 5` → `1, 3, 5` (변화 없음)
    
- 입력: `2, 4, 6` → `2, 4, 6` (변화 없음)
    

---

## 5. frontBackSplitLL 

리스트를 **앞 절반(front)**과 **뒤 절반(back)**으로 분할한다.  
노드 개수가 홀수라면 앞쪽 리스트가 하나 더 가진다.

```c
void frontBackSplitLL(LinkedList *ll, LinkedList *resultFrontList, LinkedList *resultBackList);
```

✅ 예시

- 입력: `2, 3, 5, 6, 7`  
    → frontList: `2, 3, 5`  
    → backList: `6, 7`
    

---

## 6. moveMaxToFront

리스트를 한 번 순회하여 가장 큰 값을 가진 노드를 찾아 **맨 앞**으로 옮긴다.

```c
int moveMaxToFront(ListNode **ptrHead);
```

✅ 예시

- 입력: `(30, 20, 40, 70, 50)`
    
- 출력: `(70, 30, 20, 40, 50)`
    

---

## 7. recursiveReverse

**재귀(recursive)** 방식으로 리스트를 뒤집는다.  
노드의 `next` 포인터와 `head` 포인터를 모두 갱신한다.

```c
void recursiveReverse(ListNode **ptrHead);
```

✅ 예시

- 입력: `(1, 2, 3, 4, 5)`
    
- 출력: `(5, 4, 3, 2, 1)`
    
