---
date: 2025-11-08
day_of_week: 토요일
---
---
type: daily-note
aliases:
  - 2025년 11월 08일 토요일
tags: [CS, index/daily]
---

# 📅 2025-11-08

> [!NOTE] 오늘의 생각과 할 일을 기록하는 노트입니다.

⬅️ [[2025-11-07]] | [[2025-11]] | [[2025-11-09]] ➡️

---

## ✅ 오늘의 할 일 (Today's Tasks)

> [!TIP]
> **오늘 마감**이거나 **오늘 시작**하는 모든 할 일을 자동으로 보여줍니다. 갑자기 생각난 할 일은 바로 아래에 추가하세요!

```tasks
not done
happens on or before today
group by due
short mode
hide backlink
hide task count
```
---

## 📝 오늘의 노트 (Today's Notes)

ㅋㅋㅋㅋ 알겠어, 비유 다 빼고 정확하게 말할게.

`list_entry()`는 **리스트 노드(`struct list_elem *`)의 주소를 원래 그 노드를 포함하고 있는 구조체 주소로 바꿔주는 매크로**야.  
즉, “이 `list_elem*`이 `struct thread` 안의 `elem` 멤버라면, 그걸 포함하고 있는 `struct thread*` 주소를 계산해줘” 하는 기능.

구체적으로 작동 방식은 이렇다:

```c
#define list_entry(ELEM, TYPE, MEMBER) \
  ((TYPE *) ((uint8_t *) (ELEM) - offsetof(TYPE, MEMBER)))
```

- `ELEM`: `struct list_elem *` 포인터
    
- `TYPE`: 원래 구조체 타입 (예: `struct thread`)
    
- `MEMBER`: 그 구조체 안의 멤버 이름 (예: `elem`)
    

이 매크로는 구조체 안에서 `MEMBER`가 얼마나 떨어져 있는지를 `offsetof`로 구하고,  
그만큼 `ELEM` 주소를 뒤로 빼서 구조체의 시작 주소를 얻는다.

예시로 보면:

```c
struct thread {
  int priority;
  struct list_elem elem;
};

struct list_elem *e = list_begin(&ready_list);
struct thread *t = list_entry(e, struct thread, elem);
```

이건 **리스트에 들어있는 노드 포인터 `e`를 thread 구조체 포인터 `t`로 되돌리는 것**이야.  
이후 `t->priority`처럼 원래 구조체 멤버를 바로 쓸 수 있지.

결론만 말하면:  
`list_entry()`는 “리스트 노드 포인터 → 그 노드를 가진 구조체 포인터” 변환 매크로다.  
그 이상도 이하도 아님. 

---

## ✨ 오늘 완료한 일 (Completed Today)

```tasks
done on 2025-11-08
sort by done desc
short mode
hide backlink
hide task count
```