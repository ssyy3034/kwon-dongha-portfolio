---
date: 2025-10-07
---

서로 유관한 데이터 아이템의 집합을 저장하기 위해 적합.
선언된 순서대로 메모리에 저장됨
각자의 namespace를 가짐
배열처럼 선언시에 초기화 가능
ex) 
```
struct {
    int number;
    char name[NAME_LEN + 1];
    int on_hand;
} part1 = {528, "Disk drive", 10},
  part2 = {914, "Printer cable", 5};

```

