---
date: 2025-11-13
tags: [CS]
---

프로그램이 있어야함.
실행파일이 필요하고 포맷은 ELF 포맷. 리눅스 프로그램은 핀토스에서 돌아감.

파일시스템.핀토스의 파일시스템이 뭔지. 간단한 파일 시스템 구현되어 있음.
- 코드 분석 및 개념 공부. 파일 업로드 방법 생각. root만 있음.

os가 파일 시스템의 포맷(fat32,ntfs) 이해하는법?
파일 시스템이 실제로 어떻게 포맷을 해석하는지.
 - 핀토스에선 만들어준다.
일단 파일 시스템이 읽기/쓰기는 가능. 그럼 핀토스로는 어떻게 전달할까?
그것도 pintos의 arg로 전달. 로컬 -> pintos로 올릴수 있음(그런 함수가 있을듯?)

파일->메모리 로드 -> 가상 메모리 맵 올리는거 구현해야함.(특히 stack)
IR,SP가 가르키는 위치를 잡아줘야함.
IR은 코드 시작영역(가상 메모리 주소 시작값? 조사 필요)
SP는?
시스템 콜 호출되면 시스템 콜 핸들러(커널에 있음) 실행(지정된 시스템 콜 핸들러 주소로 점프) 이때, IR도 시스템 콜 핸들러로 위치 지정?printf 구현해야함..

printf까지는 빠르게 구현>? 아니면 앞에꺼 하다가 printf부터 테스트 통과?

시스템 콜(50개) 및 프로세스 관련(fork,exec,wait) 구현

추가 과제는 메모리 파일(dup2)
첫번째 해야할거는 arg처리. 깃북 잘 읽어보기. main 읽어보기.
argument passing이 뭔지 조사.

시스템 콜 핸들러 넘버 정의해서 넘버 들어오는대로 switch-case
write 구현하면 printf가능(아마 1번)
## 64비트용 참고해야함
OOM테스트


sudo apt-get update
sudo apt-get install clang-tidy bear