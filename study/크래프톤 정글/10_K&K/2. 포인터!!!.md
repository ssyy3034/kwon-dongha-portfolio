---
date: 2025-10-02
tags: [CS]
---

대부분의 최신 컴퓨터에서는 주 메모리가 바이트 단위, 즉 8 비트 단위로 나뉨
각 바이트는 고유한 주소를 가지므로 모든 바이트들은 서로 이 주소를 통해 메모리 내에서 구분됨.
첫번째 바이트의 주소를 변수의 주소라고 부른다.
정수형 변수에 변수의 주소값을 저장할수 없어서, 특수한 포인터 변수에 저장함.
i의 주소를 포인터변수 p에 저장한다면 p는 i를 가리킨다고 부름.
표현형은
~~~
int *p
int* p

p = &i
~~~

여기서 포인터 변수 p는 변수를 가르킬수도 있지만, 개체라는 표현을 사용함.왜냐하면 변수가 아닌 메모리 공간 자체를 가르키기 때문.
i의 주소를 변수 p에 할당했으므로, p는 i를 가르키게 됨.
실질적으로 권장하는 방식은 선언과 함께 초기화.
포인터 변수가 개체를 가르키고 있다면,
~~~
printf(%d\n,*p)
~~~
로 i의 값을 출력할수 있음.

!!!
초기화하지 않은 포인터 변수에 참조 연산자를 절대 사용해서는 안됨.
~~~
int* p;
printf("%d", *p);   /*** 잘못됨 ***/
~~~
초기화하지 않은 p에 대해 값을 할당하는것도 위험.
~~~
int* p;
*p = 1; /*** 잘못됨 ***/
~~~
형만 같다면 할당 연산자가 포인터를 복사하는것도 허용.

포인터의 용도는 무엇일까?
# 📘 C 언어: 함수에서 포인터 사용하기 (입력 + 반환)

---

## 1. 함수 입력변수로서 포인터

- C 함수는 기본적으로 **값에 의한 전달(call by value)** → 원본 수정 불가.
    
- 원본을 수정하거나 여러 값을 반환하고 싶을 때 → **포인터 전달**.
    
- 예시:
    
    ```c
    void decompose(double x, long* out_int, double* out_frac) {
        *out_int = (long) x;
        *out_frac = x - *out_int;
    }
    
    long i; double d;
    decompose(3.14, &i, &d); // i=3, d=0.14
    ```
    
- `scanf("%d", &x)`도 같은 원리 → 함수가 x의 주소를 받아 직접 값 저장.
    

---

## 2. const로 입력변수 보호

- 포인터 인자는 보통 “수정 가능”으로 이해됨.
    
- **수정 의도가 없다면 const로 보호**하는 습관 권장.
    
    ```c
    void print_array(const int* arr, int size) {
        for (int i = 0; i < size; i++) {
            printf("%d\n", arr[i]); // 읽기만 가능
        }
    }
    ```
    
- 장점:
    
    - **컴파일러가 수정 시도 방지**
        
    - **함수 의도 문서화**
        
    - **최적화 가능성 증가**
        

> 원칙: "기본적으로 const, 수정 필요하면 제거"  
> 반환값에는 포인터일 때만 const 의미 있음.

---

## 3. 반환값으로서 포인터

- 함수는 포인터를 반환할 수 있다 → **원본 데이터의 위치를 가리키는 포인터를 돌려줌**.
    
- 예시: 두 정수 중 큰 값의 포인터 반환
    
    ```c
    int* get_max(int* left, int* right) {
        return (*left > *right) ? left : right;
    }
    
    int a=10, b=20;
    int* p = get_max(&a, &b); // p는 b를 가리킴
    ```
    
- **중요 규칙**
    
    - **반환 가능한 포인터**:
        
        - 외부 변수, 전역 변수
            
        - static 지역 변수
            
        - 함수 인자로 받은 변수/배열 원소
            
    - **절대 반환하면 안 됨**:
        
        - **자동 지역 변수(auto local)** 주소 → 함수 종료 시 소멸되므로 무효 포인터
            
    
    ```c
    int* f(void) {
        int i = 42;
        return &i;   // ❌ 위험: 함수 종료 시 i는 사라짐
    }
    ```
    

---

## 4. 배열과 포인터 반환

- 포인터는 배열 원소도 가리킬 수 있음.
    
- 따라서 함수가 배열 원소 중 하나를 반환하는 것도 가능.
    
    ```c
    int* find_middle(int arr[], int size) {
        return &arr[size/2]; // 중간 원소 주소 반환
    }
    ```
    
- 호출부에서 받은 포인터를 통해 배열 원소에 접근/수정 가능.
    

---

## 5. 종합 개념 요약

- **포인터 입력**: 원본 수정 + 다중 반환 가능
    
- **const 포인터 입력**: “읽기 전용” 보장 → 안전성과 의도 명확화
    
- **포인터 반환**:
    
    - 함수 외부에서 여전히 유효한 메모리만 가리키도록 설계해야 함
        
    - 자동 지역변수 주소 반환은 절대 금지
        
- **배열과 포인터**: 배열 인자는 포인터처럼 취급되며, 배열 원소 주소를 반환하는 것은 흔한 패턴
    

---

## 6. 추가 개념적 포인트

- 함수에서 포인터 반환은 사실상 **데이터 복사 비용 최소화** 전략임 → 큰 배열/구조체를 통째로 복사하지 않고, 포인터만 전달.
    
- **API 설계 습관**: 출력 매개변수는 `out_` 접두어, 읽기 전용 매개변수는 `const`로 표현 → 협업 시 의도 명확화.
    
- 포인터 반환은 강력하지만, **유효 기간(lifetime) 관리** 실패 시 가장 위험한 버그 발생 (dangling pointer).
    

---

📌 **핵심 한 줄 정리**

- 포인터 입력: 원본 수정·다중 반환
    
- const 포인터: 읽기 전용 보장
    
- 포인터 반환: 유효한 메모리만 → 자동 지역변수 주소 반환 금지
    

## 이중 포인터
